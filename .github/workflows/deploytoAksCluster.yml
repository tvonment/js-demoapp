on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: demobuehler.azurecr.io
        username: ${{ secrets.acr_demobuehler_username }}
        password: ${{ secrets.acr_demobuehler_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t demobuehler.azurecr.io/akscluster:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push demobuehler.azurecr.io/akscluster:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_akscluster_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep aksclusterb936 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace aksclusterb936`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: aksclusterb936
        container-registry-url: demobuehler.azurecr.io
        container-registry-username: ${{ secrets.acr_demobuehler_username }}
        container-registry-password: ${{ secrets.acr_demobuehler_password }}
        secret-name: aksclusterdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: aksclusterb936
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          demobuehler.azurecr.io/akscluster:${{ github.sha }}
        imagepullsecrets: |
          aksclusterdockerauth
