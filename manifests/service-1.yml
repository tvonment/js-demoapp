apiVersion: v1
kind: Service
metadata:
    name: "akscluster-e1f4"
    labels:
        app: "akscluster-e1f4"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-e1f4"