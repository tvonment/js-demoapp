apiVersion: v1
kind: Service
metadata:
    name: "akscluster-0879"
    labels:
        app: "akscluster-0879"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-0879"