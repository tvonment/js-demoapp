apiVersion: v1
kind: Service
metadata:
    name: "akscluster-8012"
    labels:
        app: "akscluster-8012"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "akscluster-8012"