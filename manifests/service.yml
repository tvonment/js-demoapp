apiVersion: v1
kind: Service
metadata:
    name: "akscluster-e985"
    labels:
        app: "akscluster-e985"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-e985"