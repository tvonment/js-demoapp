apiVersion: v1
kind: Service
metadata:
    name: "akscluster-e905"
    labels:
        app: "akscluster-e905"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-e905"