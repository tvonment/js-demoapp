apiVersion: v1
kind: Service
metadata:
    name: "akscluster-f6b1"
    labels:
        app: "akscluster-f6b1"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-f6b1"