apiVersion: v1
kind: Service
metadata:
    name: "akscluster-87e2"
    labels:
        app: "akscluster-87e2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-87e2"