apiVersion: v1
kind: Service
metadata:
    name: "akscluster-f528"
    labels:
        app: "akscluster-f528"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-f528"