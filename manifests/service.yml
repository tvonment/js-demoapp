apiVersion: v1
kind: Service
metadata:
    name: "akscluster-dd13"
    labels:
        app: "akscluster-dd13"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-dd13"