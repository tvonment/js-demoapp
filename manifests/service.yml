apiVersion: v1
kind: Service
metadata:
    name: "akscluster-b7cd"
    labels:
        app: "akscluster-b7cd"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-b7cd"