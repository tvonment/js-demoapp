apiVersion: v1
kind: Service
metadata:
    name: "akscluster-cc92"
    labels:
        app: "akscluster-cc92"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-cc92"