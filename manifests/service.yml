apiVersion: v1
kind: Service
metadata:
    name: "akscluster-5dab"
    labels:
        app: "akscluster-5dab"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-5dab"