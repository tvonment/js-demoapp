apiVersion: v1
kind: Service
metadata:
    name: "heroescluster-0403"
    labels:
        app: "heroescluster-0403"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "heroescluster-0403"