apiVersion: v1
kind: Service
metadata:
    name: "akscluster-c210"
    labels:
        app: "akscluster-c210"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-c210"